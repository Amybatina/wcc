def multiply(a, b):
    result = a * b
    return result

# Test the function:
#solution = multiply(4, 5) # Invoke multiply giving it the arguments 4 and 5

# Test the function
multiply(4, 5) # Expected: ???
